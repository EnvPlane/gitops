version: "2"

run:
  timeout: 5m
linters:
  enable: [errcheck, govet, staticcheck, ineffassign, unused, bodyclose, errorlint, gosec]
  exclusions:
    rules:
      - path: _test\.go
        linters: [gosec]
